%div.yp_banner
  = image_tag("yp.png")
%h2{:style => "margin-left:25px; text-shadow: 2px 2px 3px gray"}  Create New Category
%br

= render "left_navigation_menu"

= render(:partial => 'form', :locals => {:zone => @category})

%br